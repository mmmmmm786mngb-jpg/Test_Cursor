Проблема1:  Так поиск нельзя делать, так как мы табличную часть не передавали  в фоновые.
Как правильно придумать удаление строки в которой сделка успешно обработана?
Иначе ошибка:

Ошибка при вызове метода контекста (Удалить)
{ВнешняяОбработка.ГенераторСделокTn.МодульОбъекта(1395)}:СписокЗагрузки.Удалить(ТекСтрока);
{ВнешняяОбработка.ГенераторСделокTn.Форма.Форма.Форма(160)}:ТекОбъект.СоздатьОбъекты(Ложь);
{ВнешняяОбработка.ГенераторСделокTn.Форма.Форма.Форма(46)}:СоздатьСделкиНаСервере();

по причине:
Недопустимое значение параметра (параметр номер '1') (Колонка не принадлежит коллекции)

Проблема2:
Для генерации время коректное, а просто время выполнения если часы или минуты или секунды = 0 , то пишет ПУСТО
Также если нет ошибок то пишет не 0 а ПУСТО:

Генерация входных данных выполнялось - 0:0:23,561
  Обработано сделок: 4
  Ошибок:
  Время выполнения: ::02
