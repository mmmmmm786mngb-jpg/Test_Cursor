
1) Операция: Операция0050ПереводНКДВЗадолженностьЭмитента
Функция: ПереоцениваемыеОстаткиПоСчетам не добавил, так как не учтен момент с значением разных типов в массиве
 Предложения:
  - Это важное дополнение ускоряющее расчеты, поэтому предлагаю небольшую модификацию проверяющие чтобы типы были одинаковые

 (см. вложение и исопльзование новой функции ВозможноВыразить)

2) Операция: Операция0010УстановкаКотировок
 2.1 Если НЕ СтрокаТЧ.Актив.ВидАктива.ПринадлежитЭлементу(ПланыВидовХарактеристик.ВидыАктивов.Облигации)  - медленновато работает,
    предназначена для проверки является ли ВидАктива облигацией
    (если не облигация то обнуляется НКД: НКД = 0)

   Может использовать другой способ определить что это облигация? (кроме ВидАктива.ПринадлежитЭлементу) ?

2.2 МенеджерКотировокПоСпискуЦБ  - забыл убрать НАЧАЛОПЕРИОДА)
     А так в целом вычислять параметры лучше до запроса чем внутри запроса,
     у нас были случаи когда отказ от приведения параметра-даты внутри запроса сокращал в 10 раз время его выполнения,
     так как построитель запросов очень часто странный код делает если дата вычисляется а не используется фиксированной.
