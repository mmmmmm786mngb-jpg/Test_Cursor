.. {color:#FFAB00}дополняется{color}...
h1. Проблемные строки вечерних операций

Список пунктов оптимизации по документу «Замеры по пунктам оптимизации (вечерние операции)». Под каждым пунктом подставьте ссылку на документ решения и на тесты.

---
|| № блока || Номера операций || Код || Описание || Период выполнения ||
| **1** | 2, 6 | 0110 | Переоценка активов | 10.02 – 11.02 |
| **2** | 4, 8 | 0010 | Установка котировок | 12.02 – 13.02 |
| **3** | 5, 7. 10 | — | Прочее | 16.02 |
| **4** | 1 | 1000 | РСЧ СЧА | 17.02 – 18.02 |
| **5** | 3, 9 | 0040 | Начисление КД | 19.02 – 20.02 |
---

**1. Расчет СЧА/РСА (запрос УУ)**  
Модуль: Документ.РасчетСЧА_РСА.МодульМенеджера, строка 505  
Код: `РезультатУУ = ЗапросУУ.Выполнить();`  
Вызовов: 3 532, время: 2 883,72 с (11,99%)

Описание: ПолучитьСуммыРСАСЧА(). Вызывается из Операция_1000_РасчетРСА_СЧА() через ИсполняемыеПроцедурыЗакрытияПериода. Запрос выполняется до проведения документа, получает данные для расчета СЧА и РСА для УУ.

Цепочка: Операция_1000_РасчетРСА_СЧА → ИсполняемыеПроцедурыЗакрытияПериода → Документ.РасчетСЧА_РСА → ПолучитьСуммыРСАСЧА(), стр. 505.

- Решение:
- Тесты:

---

**2. Переоценка активов (ЗаполнитьТЧ)**  
Модуль: Документ.ПереоценкаАктивов.МодульОбъекта, строка 671  
Код: `Результат = Запрос.Выполнить();`  
Вызовов: 3 528, время: 2 471,83 с (10,27%)

Описание: ЗаполнитьТЧ(). Вызывается из Операция_0110_ПереоценкаАктивов(). Запрос выполняется до проведения документа при заполнении табличной части, получает данные об активах для переоценки.

Цепочка: Операция_0110_ПереоценкаАктивов → Документ.ПереоценкаАктивов → ЗаполнитьТЧ(), стр. 671.

- Решение:
- Тесты:

---

**3. Начисление КД — облигации особой периодичности**  
Модуль: Документ.НачислениеКупонногоДохода.МодульОбъекта, строка 269  
Код: `ТаблицаАктивов = ФильтроватьОблигации...(Запрос.Выполнить().Выгрузить());`  
Вызовов: 3 528, время: 2 316,44 с (9,63%)

Описание: ЗаполнитьКупоннымиПериодами(). Вызывается из Операция_0040_НачислениеКД(). Запрос получает облигации с особой периодичностью, результат фильтруется.

Цепочка: Операция_0040_НачислениеКД → Документ.НачислениеКупонногоДохода → ЗаполнитьКупоннымиПериодами(), стр. 269.

- Решение:
- Тесты:

---

**4. Установка котировок ЦБ**  
Модуль: Документ.УстановкаКотировокЦБ.МодульОбъекта, строка 299  
Код: `Результат = Запрос.Выполнить();`  
Вызовов: 3 528, время: 1 823,74 с (7,58%)

Описание: ЗаполнитьКотировки(). Вызывается из Операция_0010_УстановкаКотировок(). Запрос выполняется до проведения документа, получает данные для установки котировок ценных бумаг.

Цепочка: Операция_0010_УстановкаКотировок → Документ.УстановкаКотировокЦБ → ЗаполнитьКотировки(), стр. 299.

- Решение:
- Тесты:

---

**5. Поиск в таблице дней исключений**  
Модуль: ОбщийМодуль.КалендарныеГрафики.Модуль, строка 310  
Код: `НайденныйДень = ТаблицаДнейИсключений.Найти(День,"Дата");`  
Вызовов: 2 946 871, время: 1 510,54 с (6,28%)

Описание: Вызывается при работе с календарными графиками (ОбщийМодуль.КалендарныеГрафики). Поиск дня в таблице исключений в функции ОпределитьТипДня.

Цепочка: Регламенты (переоценка, КД, котировки и др.) → КалендарныеГрафики (ПолучитьТаблицуРегистраКалендарь, ОпределитьТипДня, ПолучитьСледующуюРабочуюДату) → стр. 310.

- Решение: [ [^Решение_оптимизация_строка_310_КалендарныеГрафики.md] ]
- Тесты:

---

**6. Переоценка активов — второй запрос (УУ)**  
Модуль: Документ.ПереоценкаАктивов.МодульОбъекта, строка 1784  
Код: `СтруктураВозврата.Вставить("...", Запрос.Выполнить().Выгрузить());`  
Вызовов: 2 746, время: 1 456,43 с (6,05%)

Описание: ЗаполнитьПоКотировкамУУ(). Вызывается из Операция_0110_ПереоценкаАктивов(). Запрос выполняется до проведения документа, получает данные об активах для УУ.

Цепочка: Операция_0110_ПереоценкаАктивов → Документ.ПереоценкаАктивов → ЗаполнитьПоКотировкамУУ(), стр. 1784.

- Решение:
- Тесты:

---

**7. Правила округления котировок**  
Модуль: Справочник.ПравилаОкругленияКотировок.МодульМенеджера, строка 70  
Код: `Результат = Запрос.Выполнить();`  
Вызовов: 215 460, время: 1 378,03 с (5,73%)

Описание: Запрос в функции ПравилаОкругленияПоВидуАктива (модуль менеджера справочника). Выполняется при расчетах котировок, переоценке, НКД и др.

Цепочка: Операция_0010_УстановкаКотировок, Операция_0110_ПереоценкаАктивов, Операция_0040_НачислениеКД и др. → расчеты котировок/переоценки → Справочник.ПравилаОкругленияКотировок.ПравилаОкругленияПоВидуАктива(), стр. 70.

- Решение: [ [^Решение_оптимизация_строка_70_ПравилаОкругленияКотировок.md] ]
- Тесты:

---

**8. Методы расчета котировок**  
Модуль: Перечисление.МетодыРасчетаКотировок.МодульМенеджера, строка 156  
Код: `Результат = Запрос.Выполнить();`  
Вызовов: 1 861, время: 1 206,60 с (5,02%)

Описание: Запрос к перечислению методов расчета котировок в модуле менеджера перечисления.

Цепочка: Операция_0010_УстановкаКотировок → УстановкаКотировокЦБ / УстановкаКотировокЦБПоПулам (ЗаполнитьКотировки) → РасчетныеКотировкиЦенныхБумаг → ОпределитьКотировкиПоБалансовойСтоимости (и др.), стр. 156.

- Решение:
- Тесты:

---

**9. Начисление КД — второй запрос**  
Модуль: Документ.НачислениеКупонногоДохода.МодульОбъекта, строка 2211  
Код: `Результат = Запрос.Выполнить().Выгрузить();`  
Вызовов: 1 691, время: 1 126,18 с (4,68%)

Описание: Второй запрос в процедуре начисления купонного дохода. Получает дополнительные данные для расчета купонного дохода.

Цепочка: Операция_0040_НачислениеКД → Документ.НачислениеКупонногоДохода → (процедура с запросом), стр. 2211.

- Решение:
- Тесты:
